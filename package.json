{
  "name": "buildfun",
  "version": "1.0.0",
  "private": true,
  "description": "buildfun.co — static site for the Benny and Sh!thead card games",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "check:cache-bump": "node scripts/check-cache-bump.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "jsdom": "^29.1.1",
    "playwright": "^1.56.1",
    "vitest": "^4.1.7"
  }
}
